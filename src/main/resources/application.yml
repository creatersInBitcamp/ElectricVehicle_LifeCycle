spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://electriccardb.cxzuzyxcseji.ap-northeast-2.rds.amazonaws.com/electriccardb
    username: electriccardb
    password: electriccardb
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
